//css로 어떻게든 hover기능을 만들려고 했으나...css의 심오함 때문에 실패했다
//모달을 관리한 것처럼 커스텀훅을 만들어서 관리를 해주자
//그냥 보였다가 안보였다만 하면 되니깐 bool값으로 주면될듯

// import { useState } from "react";

// const useOnMouse = () => {
//   const [isHovering, setIsHovering] = useState(false);
//   const show = (data) => {
//     setIsHovering(true);
//   };

//   const out = (data) => {
//     setIsHovering(false);
//   };

//   return { isHovering, show, out };
// };

// export default useOnMouse;
